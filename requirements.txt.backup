# Django Web Framework
Django>=5.2.3

# Database and ORM (Django includes SQLite by default)
# No additional database packages needed for SQLite

# Authentication and Security
PyJWT>=2.8.0

# AWS S3 Integration (Cloudflare R2 compatible)
boto3>=1.38.37

# Real-time WebSocket support
channels>=4.2.2

# Environment variables management
python-dotenv>=1.1.0

# HTTP requests
requests>=2.32.4

# Image processing
Pillow>=10.0.0

# Windows-specific packages (for vendor client executable)
pywin32>=306

# System monitoring (for vendor client)
psutil>=5.9.0

# Firebase Admin SDK (for authentication)
firebase-admin>=6.4.0

# WebSocket client (for real-time features)
websocket-client>=1.8.0

# Additional WebSocket support
websocket>=0.2.1

# WSGI HTTP Server for production
gunicorn>=21.2.0

# Static file serving for production
whitenoise>=6.6.0

# Note: The following are built-in Python modules and don't need installation:
# - os, sys, time, json, datetime, uuid, random, re, base64, io, math
# - threading, subprocess, tempfile, signal, asyncio, logging, glob, pathlib
# - queue, collections, dataclasses, urllib.parse, platform, argparse
# - traceback, typing, concurrent.futures
